# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=authdb

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# LLM Configuration (choose one)

# Option 1: Local Ollama (Default - No API key needed)
OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
OLLAMA_MODEL=llama3.2

# Option 2: OpenAI (Cloud - requires API key)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Notes:
# - If OLLAMA_BASE_URL is set, it will use local Ollama
# - If OPENAI_API_KEY is set (and no Ollama), it will use OpenAI
# - Import feature works with either option
# - Ollama is free and runs locally
# - OpenAI requires paid API key but may be more accurate
